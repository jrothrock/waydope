import { Component, OnInit } from '@angular/core';
import { Http } from '@angular/http';

@Component({
  selector: 'seller_apply',
  templateUrl: 'seller.apply.component.html',
})

export class SellerApplyComponent implements OnInit {
    constructor(){};
	ngOnInit(){};
}